# Reference direct/runtime dependencies from the development environment.
# Python 3.13 recommended; numpy 2.5.2 requires Python >=3.12.
# Use a fresh environment with only one OpenCV package (not headless/contrib).
ultralytics==8.4.138
opencv-python==5.0.0.93
numpy==2.5.2
torch==2.13.0
torchvision==0.28.0
# Required by the existing tracking backend; avoid an implicit first-run install.
lap==0.5.13
